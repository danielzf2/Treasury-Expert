[pytest]
testpaths = tests
pythonpath = . server casada-tool
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration tests using FastAPI TestClient
